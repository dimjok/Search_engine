import org.apache.lucene.morphology.LuceneMorphology;
import org.apache.lucene.morphology.english.EnglishLuceneMorphology;
import org.apache.lucene.morphology.russian.RussianLuceneMorphology;
import searchengine.LemmaAnalyzer.LemmaAnalyzer;

import java.io.IOException;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

public class Main {
    public static void main(String[] args) throws IOException {


//            LemmaAnalyzer lemmaAnalyzer = new LemmaAnalyzer();
//            HashMap<String, Integer> hashMap = (HashMap<String, Integer>) lemmaAnalyzer.collectLemmas("Создайте класс с методом, который будет принимать в качестве параметра текст и возвращать перечень лемм для каждого слова в этом тексте (за исключением междометий, " +
//                "союзов, предлогов и частиц — см. ниже) и количество упоминаний каждой такой леммы в переданном тексте. " +
//                "Ниже описаны детали реализации данного метода.");
//            for (Map.Entry<String, Integer> entry : hashMap.entrySet()) {
//                System.out.println(entry.getKey() + entry.getValue());
//            }
    }
}
